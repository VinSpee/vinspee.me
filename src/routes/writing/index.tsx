import { component$ } from '@builder.io/qwik';

export default component$(() => {
  return (
    <section>
      <h1>Writing</h1>
      <p>
        The original site sourced posts from Medium. As part of this migration to Qwik City, fetching and
        displaying external content can be implemented with server loaders and endpoints. For now, this is a
        placeholder page.
      </p>
    </section>
  );
});
